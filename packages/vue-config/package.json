{
  "name": "@pinala/eslint-config",
  "version": "1.0.0",
  "packageManager": "pnpm@7.0.0",
  "private": true,
  "license": "MIT",
  "author": "pinala <https://github.com/pinala/>",
  "scripts": {
    "lint": "eslint .",
    "prepare": "pnpm -r run stub",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public"
  },
  "devDependencies": {
    "@pinala/eslint-config": "*",
    "bumpp": "^7.1.1",
    "eslint": "^8.14.0",
    "typescript": "^4.6.4"
  }
}
